[build]
  publish = "."
  functions = "netlify/functions"

# 1. 首先，精确匹配 API 路径
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true  # 确保强制生效

# 2. 然后，通用回退规则（必须放在最后）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200